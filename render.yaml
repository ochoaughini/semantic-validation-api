services:
  - type: web
    name: semantic-validation-api
    env: docker
    plan: starter
    region: oregon
    healthCheckPath: /health
    rootDirectory: .
    envVars:
      - key: PORT
        value: 8080
